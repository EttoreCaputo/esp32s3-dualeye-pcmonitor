# Read-only token from CoolerControl → Access Protection → Create Token.
# The UI shows the value once (cc_<uuid>). Paste it below.
# This example is safe to commit; copy it to coolercontrol.env, which stays local.
COOLERCONTROL_TOKEN=
